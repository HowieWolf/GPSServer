新增设备和围栏的model

完成设置围栏

修改围栏的逻辑，把他放在每个位置信息的数据库
position表新增字段inRail，表示是否在围栏中

修改
1新连接的设备需要查询自己的设备信息，如果是新设备，则添加设备到数据库
2处理定位数据包要判断该位置是否在围栏中

问题遗留
新增围栏时，如果已经存在，则不进行添加
如果是同心，则仅仅修改已存在的围栏
这些逻辑放在APP端，服务器确保每一个新增都是不存在的