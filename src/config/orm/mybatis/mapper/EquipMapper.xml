<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="EquipMapper">

	<insert id="addEquip">
		insert into equipment (id , phone , name , uId)
		values (#{id},#{phone},#{name},#{uId})
	</insert>

	<update id="updateEquip">
		update equipment
		set
		name = #{name},
		phone = #{phone},
		uId = #{uId}
		where id = #{id}
	</update>
	
	<update id="updateEquipOnlyUid">
		update equipment
		set
		uId = 0
		where id = #{id}
	</update>

	<select id="selectEquipById" resultType="Equipment">
		select * from equipment
		where id = #{id}
	</select>

	<select id="selectEquipByUid" resultType="Equipment">
		select * from equipment
		where uId = #{uId} order by id
	</select>

</mapper>